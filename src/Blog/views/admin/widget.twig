<div class="col-sm-4">
	<div class="card">
		<div class="card-body">
			<p class="card-text">
				{% if count > 1 %}
					{{count}}
				{% endif %}
				Articles
			</p>
			<a href="{{ path('blog.admin.index')}}" class='btn btn-primary'>Gérer les articles</a>
		</div>
	</div>
</div>
